# Kernel module build artifacts
driver/*.o
driver/*.ko
driver/*.mod
driver/*.mod.c
driver/*.mod.o
driver/.*.cmd
driver/*.o.cmd
driver/modules.order
driver/Module.symvers
driver/.module-common.o
driver/.tmp_versions/

# Scratch capture/diagnostic dumps captured while iterating in driver/
# (real captures belong in captures/; see CLAUDE.md repo layout).
driver/*.log

# Editor/OS noise
*~
.DS_Store

# Vendor RE source material — keep out of the distributed repo (clean-room).
# Facts are carried into the authored spec/ instead; see CLAUDE.md.
vendor-reference/

# Compiled bench tools (sources are tools/fcp_*.c; build them where you need them)
tools/fcp_cap_read
tools/fcp_cfg_read
tools/fcp_meter_watch
tools/fcp_mux_probe
